import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class EncryptionService {
  // End-to-End Encryption features removed as per user request.
  // This service is currently a placeholder to avoid breaking potential accidental imports.
  constructor() {}
}
